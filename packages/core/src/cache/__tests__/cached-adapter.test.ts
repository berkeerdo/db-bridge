import { describe, it, expect } from 'vitest';

import { CachedAdapter } from '../cached-adapter';

describe('CachedAdapter', () => {
  it('should be importable', () => {
    expect(CachedAdapter).toBeDefined();
  });
});
